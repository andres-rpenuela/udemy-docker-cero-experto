# TODO tarea

#version: '3'
# docker compose up -d
# docker compose down
# docker compose down --volumes

# Ejecutar un docker compose con otro nombre
# 1º Generar la imagen a partir del dockery subirla a docker hub
# 2 docker compose -f docker-compose-pro.yml up  (o) docker compose -fdocker-compose-pro.yml up -d
# 
services:
#  db:
#    image: postgres:14.3
#    restart: always
#    ports:
#      - "5432:5432"
#    environment:
#      POSTGRES_PASSWORD: ${DB_PASSWORD}
#      POSTGRES_DB: ${DB_NAME}
#    container_name: ${DB_NAME}
#    volumes:
#      - postgres-db:/var/lib/postgresql/data
  
  # Contenedor para la aplicacion
  app:
#    depends_on:
#      - db
#    image: colera/teslo-shop:1.0.0
    image: colera/teslo-shop:1.1.0
    container_name: teslo-bakend
    # Le del fichero .evn
    ports:
      - ${PORT}:${PORT}
    # Carga las variables de entorno definidas en el .env, al contendor
    environment:
      APP_VERSION: ${APP_VERSION}
      STAGE: ${STAGE}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_USERNAME: ${DB_USERNAME}
      PORT: ${PORT}
      HOST_API: ${HOST_API}
      JWT_SECRET: ${JWT_SECRET}



volumes:
  postgres-db:
    external: false